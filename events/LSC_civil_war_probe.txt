# TEMPORARY DEBUG HARNESS — DO NOT COMMIT
#
# Fired by common/on_actions/LSC_civil_war_probe_on_actions.txt.
#
# Phase 4.2A: audited the real installed HOI4 (v1.19.2.0) on_actions files.
# common/on_actions/00_on_actions.txt has the actual Paradox comments:
#   on_civil_war_end / on_annex:  "ROOT is winner, FROM gets annexed"
#   on_capitulation:              "ROOT is capitulated country, FROM is winner"
# These two pairs are INVERSE of each other.
#
# Phase 4.2B: hardened per instruction — each event now requires CZL (as
# ROOT or FROM) to be involved before doing anything at all, so these hooks
# stay silent for any unrelated civil war happening to some other AI country
# on the map. Still only ever logs and sets debug flags — never annexes,
# never changes government, never touches ownership, never force-concludes
# a war.
#
# Phase 4.2C: also requires the global flag LSC_debug_civil_war_probe_active
# (set by CZL_civil_war_probe.txt's Test 1/Test 2, cleared by
# LSC_debug_clear_probe_flags). Without it, these hooks stay silent even for
# a genuine CZL civil war — this is what stops the temporary harness from
# reacting to a REAL future succession war if this file is still present
# when that happens.
#
# Also confirmed real (events/NewsEvents.txt, news.64/65/162): the vanilla
# Spanish Civil War does NOT actually resolve via these on_action hooks at
# all — it resolves via a polled news_event trio checking
# `SPR = { has_civil_war = no }` plus government agreement across every
# country sharing original_tag = SPR, with a third "inconclusive" event as
# an anti-softlock fallback. That pattern is the real candidate for Lesc's
# eventual LSC_kael_victory/LSC_selene_victory/LSC_alvaro_victory logic —
# NOT implemented here, this file still only probes the four hooks
# themselves for observation.

add_namespace = LSC_civil_war_probe

country_event = {
	id = LSC_civil_war_probe.90
	title = LSC_civil_war_probe.90.t
	desc = LSC_civil_war_probe.90.d
	picture = GFX_news_event_generic_parliament
	is_triggered_only = yes
	hidden = yes

	trigger = {
		has_global_flag = LSC_debug_civil_war_probe_active
		OR = {
			ROOT = { original_tag = CZL }
			FROM = { original_tag = CZL }
		}
	}

	immediate = {
		set_country_flag = LSC_debug_on_civil_war_end_fired
		log = "[GetDateAsString] LSC_DEBUG on_civil_war_end FIRED for CZL-related war. ROOT=[ROOT.GetName]([ROOT.GetTag]) FROM=[FROM.GetName]([FROM.GetTag]). Expected per vanilla: ROOT=winner, FROM=annexed."
	}

	option = {
		name = LSC_civil_war_probe.90.a
	}
}

country_event = {
	id = LSC_civil_war_probe.91
	title = LSC_civil_war_probe.91.t
	desc = LSC_civil_war_probe.91.d
	picture = GFX_news_event_generic_parliament
	is_triggered_only = yes
	hidden = yes

	trigger = {
		has_global_flag = LSC_debug_civil_war_probe_active
		OR = {
			ROOT = { original_tag = CZL }
			FROM = { original_tag = CZL }
		}
	}

	immediate = {
		set_country_flag = LSC_debug_before_annexation_fired
		log = "[GetDateAsString] LSC_DEBUG on_civil_war_end_before_annexation FIRED for CZL-related war. ROOT=[ROOT.GetName]([ROOT.GetTag]) FROM=[FROM.GetName]([FROM.GetTag]). FROM should still exist right now."
	}

	option = {
		name = LSC_civil_war_probe.91.a
	}
}

country_event = {
	id = LSC_civil_war_probe.92
	title = LSC_civil_war_probe.92.t
	desc = LSC_civil_war_probe.92.d
	picture = GFX_news_event_generic_parliament
	is_triggered_only = yes
	hidden = yes

	trigger = {
		has_global_flag = LSC_debug_civil_war_probe_active
		OR = {
			ROOT = { original_tag = CZL }
			FROM = { original_tag = CZL }
		}
	}

	immediate = {
		set_country_flag = LSC_debug_on_capitulation_fired
		log = "[GetDateAsString] LSC_DEBUG on_capitulation FIRED for CZL-related war. ROOT=[ROOT.GetName]([ROOT.GetTag]) FROM=[FROM.GetName]([FROM.GetTag]). Expected per vanilla: ROOT=capitulated, FROM=winner — INVERSE of the other three hooks."
	}

	option = {
		name = LSC_civil_war_probe.92.a
	}
}

country_event = {
	id = LSC_civil_war_probe.93
	title = LSC_civil_war_probe.93.t
	desc = LSC_civil_war_probe.93.d
	picture = GFX_news_event_generic_parliament
	is_triggered_only = yes
	hidden = yes

	trigger = {
		has_global_flag = LSC_debug_civil_war_probe_active
		OR = {
			ROOT = { original_tag = CZL }
			FROM = { original_tag = CZL }
		}
	}

	immediate = {
		set_country_flag = LSC_debug_on_annex_fired
		log = "[GetDateAsString] LSC_DEBUG on_annex FIRED for CZL-related war. ROOT=[ROOT.GetName]([ROOT.GetTag]) FROM=[FROM.GetName]([FROM.GetTag]). Expected per vanilla: ROOT=winner, FROM=annexed."
	}

	option = {
		name = LSC_civil_war_probe.93.a
	}
}
