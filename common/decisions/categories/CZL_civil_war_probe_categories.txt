# TEMPORARY DEBUG HARNESS — DO NOT COMMIT
#
# Crash fix: common/decisions/CZL_civil_war_probe.txt defined its category's
# icon/allowed/visible inline, in the same block as the decisions, instead
# of in a separate common/decisions/categories/ file the way
# CZL_imperial_fracture_category correctly does. The game's decision loader
# does not recognize an unregistered category id — it fell through to a
# trigger-parsing path and every token after it in that file was reported
# as "unexpected", which is what actually crashed the game before the menu.
# This file is the missing category registration. Delete it together with
# the rest of the Phase 4.2/4.2A/4.2B/4.2C probe harness.

LSC_debug_civil_war_probe_category = {
	icon = GFX_decision_category_power_struggle

	allowed = {
		tag = CZL
	}

	visible = {
		tag = CZL
		has_country_flag = LSC_debug_civil_war_probe
	}
}
